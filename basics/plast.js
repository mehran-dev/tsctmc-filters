true == (function () {






    if ((pc) && (pl)) {
        const res = 0.95 * (pc) >= (pl) && (pc) / (pl) <= 1.07

        if (res) {

            if ((l18).indexOf("کچاد") == 0) {

                console.log("pc =", (pc), "pl = ", (pl), "result is =", +amount * (pc) >= (pl), (l18))
            }
        }

        return res
    } else {
        return false
    }
})()